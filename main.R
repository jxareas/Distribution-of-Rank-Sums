# Loading Dependencies ----------------------------------------------------

library(shiny) # for shiny
library(ggplot2) # for ggplot
library(dplyr) # for mutate and pull
library(grid) # for x - axis tick lengths


# Run the application ----------------------------------------------------
runApp("app")